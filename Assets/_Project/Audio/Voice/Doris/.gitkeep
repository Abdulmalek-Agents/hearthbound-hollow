# Doris (warm, unhurried elderly baker) voice clips land here after
# running `bash Tools/generate_voices.sh`. Filenames match the lineIds
# threaded into Mission01Director.cs (e.g. doris_m1_greet_01.wav).
#
# Piper voice:    en_US-lessac-medium  (length_scale 1.15)
# Total lines:    55 (full Mission 1 coverage)
# Format:         22 kHz mono PCM16 WAV
