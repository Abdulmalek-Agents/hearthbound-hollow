# Pickle (the cat narrator). Lines render in DialogueUI's italic /
# dim-amber / no-portrait mode (see playtest commit 5/6).
#
# Piper voice:    en_US-amy-medium  (length_scale 0.95 — quicker, sly)
# Total lines:    6 (M1 + M2 hooks — italic asides)
# Format:         22 kHz mono PCM16 WAV
#
# Pickle does NOT get a procedural mumble bank (her lines are
# voiced); MumbleVoicePlayer will suppress when a real VoiceLibrarySO
# clip resolves (Phase 32.10).
