# Marin (the player's predecessor as Keeper — never seen, only her
# whispered notes are heard via the MarinNoteInteractable).
#
# Piper voice:    en_US-amy-medium  (length_scale 1.20 — slow, soft)
# Total lines:    4 (notes scattered across Lane / Hollow / workbench)
# Format:         22 kHz mono PCM16 WAV
