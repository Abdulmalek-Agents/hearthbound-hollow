# Voice clips root. Per-character subfolders below; per D-058 + D-059
# the .wav files themselves are user-generated by Tools/generate_voices.sh
# (Piper TTS) and NOT committed. The Phase32_VoiceLibraryBuilder editor
# utility scans this tree recursively and rebuilds
# Resources/HearthboundVoiceLibrary.asset.
#
# See Docs/VOICE_CASTING.md for the canonical casting table.
